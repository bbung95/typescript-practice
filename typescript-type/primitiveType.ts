/**
 * Primitive (원시값)
 *
 * - 불변
 * - 객체가 아닌 값들
 */

const str: string = "123";
const num: number = 123;
const isBoolean: boolean = false;

const big: bigint = 100n;

// 타입으로 값을 가지도록 할 수 있음
const str2: "123" = "123";
const num2: 123 = 123;
const isBoolean2: true = true;
