/**
 * Literal Type
 *
 * - let은 재할당이 가능하기에 설정한 타입을 가짐
 * - const는 재할당이 불가능하기에 값 자체를 타입으로 가짐
 */

let letString = "hello";

// type string
letString;

const constString = "Hello";

// type "Hello"
constString;
